using UnityEngine;

[System.Obsolete("Zastąpione przez ShipController — nie dodawaj na prefabie statku.")]
public class Move : MonoBehaviour
{
    void Update()
    {
        // Wyłączone — konflikt ze ShipController.
    }
}
